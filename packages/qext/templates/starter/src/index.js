import {
	initialProperties,
	template,
	definition,
	controller,
	paint,
	resize,
} from "./methods"
import "./style.css"

window.define([], function() {
	return {
		initialProperties,
		template,
		definition,
		controller,
		paint,
		resize,
	}
})
